import React from "react";
import { Grid } from "@mui/material";

const CenteredRowGridContainer = () => (
  <Grid
    container
    spacing={1}
    direction="row"
    justifyContent="center"
    alignItems="center"
  />
);

export default CenteredRowGridContainer;
